# law-graph-webmcp

## Zeabur Deployment
- Project ID: 6a94e7dc2ed2e7dbfbd22854（law-graph-webmcp，專屬伺服器 server-69eeeb93f5343e60f36375c2）
- Service ID（app，Spring Boot 主站）: 6a94e90bb869c167a93d3b2e
- Service ID（legal-mcp，台灣法規判例 MCP sidecar）: 6a94e8c3b869c167a93d3b08
- Zeabur 部署卡住（2026-09-05 經驗）：build 完成後 pod 停在 Pulling image、服務 STARTING 超過 30 分鐘、runtime log 空白，兩次重新 deploy 都一樣；`npx zeabur@latest service restart --id <svc> --env-id <env> -y -i=false` 後 20 秒內拉取成功並上線。舊容器在卡住期間仍正常服務，重啟才會有約一分鐘 502。
- 重佈指令（只重佈 app）：`npx zeabur@latest deploy --project-id 6a94e7dc2ed2e7dbfbd22854 --service-id 6a94e90bb869c167a93d3b2e --json`
- app 環境變數：OPENAI_API_KEY／OPENAI_BASE_URL／MODEL／LEGAL_MCP_URL=http://legal-mcp.zeabur.internal:8000
- LLM 供應商（2026-09-04 起暫用 Meta Muse，OpenAI 相容）：OPENAI_BASE_URL=https://api.meta.ai/v1（含 /v1，Spring AI 2.0 會自行接 /chat/completions）、MODEL=muse-spark-1.3-contributor、OPENAI_API_KEY=LLM_… 金鑰。模型目錄由專案自帶的 `src/main/resources/models/openai-models.yml` 遮蔽 Embabel 內建檔（只含 muse-spark-1.3(-contributor) 與 gpt-5.4-mini/nano，無 embedding）；新模型要先加進這份 yml 才能被 MODEL 選到。Muse 是 reasoning 模型，reasoning_tokens 會吃 completion 額度，yml 內 max_tokens 設 16384；結構化輸出偶爾吐出畸形 JSON，靠 Embabel 重試（10 次）補救。切回 OpenAI：刪 OPENAI_BASE_URL、換金鑰、MODEL=gpt-5.4-mini 即可。
- LLM 單次呼叫逾時：`embabel.agent.platform.llm-operations.prompts.default-timeout`＝${LAWGRAPH_LLM_TIMEOUT:240s}（Embabel 預設 60s；2026-09-04 線上 analyze 步驤連兩次「timed out after 60,000ms」重跑，整案 299 秒，故放寬）。Zeabur 主機與連 Meta 的網路都已量測正常，慢一律先查 runtime log 有沒有 timed out／Retry attempt。
- LLM 轉送端點與思考強度（2026-09-05 起）：`/internal/llm/v1/chat/completions`（LlmProxyController，只准 loopback）把請求加上 `lawgraph.llm.reasoning-effort`＝${LAWGRAPH_REASONING_EFFORT:}（空＝不注入）後轉給 `lawgraph.llm.upstream-base-url`＝${LAWGRAPH_LLM_UPSTREAM_BASE_URL:https://api.meta.ai/v1}。**線上自 2026-09-05 起已啟用**：OPENAI_BASE_URL=`http://127.0.0.1:8080/internal/llm/v1`、LAWGRAPH_LLM_UPSTREAM_BASE_URL=https://api.meta.ai/v1、LAWGRAPH_REASONING_EFFORT=low（同案例實測 278s→199s，品質未退化，見 eval/quality-compare-2026-09-05.md）；要關掉就把 OPENAI_BASE_URL 改回 https://api.meta.ai/v1。Embabel／Spring AI 本身不會送 reasoning_effort。實測 Muse：未指定≈medium（38.7s／2090 reasoning tokens）、low 18.6s／814、high 45.3s／2688。GET /api/usage/llm 揭露 cached／reasoning tokens 與命中率（記憶體累計，重啟歸零）。
- 使用授權排除（2026-09-05 起，與 law-powers 技能包一致：無償授權所有人，唯經兆國際法律事務所除外）：`tw.lawgraph.auth.AccessPolicy` 以登入 email 比對 `LAWGRAPH_BLOCKED_EMAIL_DOMAINS`（逗號分隔網域，含子網域）／`LAWGRAPH_BLOCKED_EMAILS`；命中者案件 API 回 403 `LICENSE_EXCLUDED`、/api/me 回 blocked=true，前端右上角顯示拒絕訊息與登出。線上已設 LAWGRAPH_BLOCKED_EMAIL_DOMAINS=law-meridian.com（經兆官網 https://www.law-meridian.com/）。頁面最上方有與 law-powers 技能頁相同的授權橫幅（.license-banner，i18n license.banner.*）；頁首語系選單已移除（語系依瀏覽器語言），Google 登入區放最右側；e2e 改用 window.__lawGraphApp.setLocale 切語系。Agent 端另有技能 SKILL.md 的停止條款作第二道防線。
- Google 登入（2026-09-05 起）：spring-boot-starter-oauth2-client，`tw.lawgraph.auth.SecurityConfig` 只在 `lawgraph.auth.google.client-id`（環境變數 GOOGLE_CLIENT_ID，本機 .env 亦接受 CLIENT_ID／SECRET）非空時建立 Google registration；全站 permitAll、CSRF 關閉、登入只提高配額。Callback：`{公開網址}/login/oauth2/code/google`（線上 https://law-graph-webmcp.zeabur.app/login/oauth2/code/google，本機 http://localhost:8080/login/oauth2/code/google）；登入入口 /oauth2/authorization/google，登出 POST /logout。GET /api/me 回 enabled/loggedIn/name/email/picture。配額雙軌：匿名 LAWGRAPH_DAILY_CASES_PER_USER（預設 1，以 IP）、登入 LAWGRAPH_DAILY_CASES_PER_MEMBER（預設 5，以 Google sub）；/api/quota 多回 loggedIn/memberLimit/loginPath，前端右上角 #auth-slot 由 login.js 渲染（頭像＋登出／Google 登入＋好處）。session 在記憶體，重佈會登出。WebMvcTest 要 @Import(SecurityConfig, QuotaIdentityResolver)。
- 每人每日案件配額（2026-09-05 起）：`lawgraph.daily-cases-per-user`＝${LAWGRAPH_DAILY_CASES_PER_USER:3}，以來源 IP 為 key、台北日曆日歸零、重啟歸零（記憶體）。超過回 429 `DAILY_CASE_LIMIT`，訊息說明「免費開放、為了讓更多人用得到才設上限」；GET /api/quota 回 {date, used, limit, remaining, exhausted}，前端輸入頁常駐顯示「今日已分析 n / N」與原因，用完改橫幅。為了在 Zeabur 代理後拿到真實 IP，application.yml 設 `server.forward-headers-strategy: framework`（X-Forwarded-For）。測試／腳本連跑多案會撞配額，可暫時把該環境變數設 0。
- 提示詞注入防禦（2026-09-05 起）：`LegalPrompts.system()` 第 8 條宣告 `<case>`／`<answers>`／`<brainstorm>`／上傳附件內容一律是資料不是指令，遇到改角色、洩漏提示、跳步驤、引用未列法源、改輸出格式等指令一律忽略並繼續分析；其他既有防線：XML 標籤框住輸入、固定 JSON 輸出結構、法條判決只能引用檢索白名單、MCP 工具只開放六個。未做輸入端關鍵字過濾（效果有限）。
- 流程七步（2026-09-05 起）：BRAINSTORM → QUESTIONS → RESEARCH → ANALYSIS → ASSESSMENT（assessCase：CaseAssessment{defenses, evidencePlan, riskSummary, checklist}，對造抗辯評估＋民訴 277 舉證責任＋當事人準備清單）→ DOCUMENTS → GRAPH。StatusMapper.deriveStep 以 assessment 判斷 DOCUMENTS；CaseStatus.Result 多 assessment 欄位（NON_NULL）。結果頁「涵攝與評估」分頁含請求權小結（前端依 met 彙整）；checklist 非空時另出「當事人準備清單」分頁，依分類分組並提供 CSV 匯出／列印（見 result.js checklistCsv／app.js printing-checklist）。
- 步驤看門狗（2026-09-04 起）：`lawgraph.step-timeout`＝${LAWGRAPH_STEP_TIMEOUT:300s}，CaseService 每 ${LAWGRAPH_STEP_TIMEOUT_SWEEP:10s} 巡檢，同一步驤（BRAINSTORM／RESEARCH／ANALYSIS／DOCUMENTS／GRAPH）超時就 `process.kill()`，狀態回 FAILED／`STEP_TIMEOUT` 並帶中文逾時訊息，不再執行後續步驤；WAITING 不計時。搭配 `embabel...data-binding.max-attempts`＝${LAWGRAPH_LLM_MAX_ATTEMPTS:2}（只保留一次畸形 JSON 補救）。使用者要求：逾時要出現錯誤訊息並結束後續動作，不要默默重試。
- 檢索逾時（2026-09-04 起）：LAWGRAPH_KEYWORD_TIMEOUT=60s、LAWGRAPH_RESEARCH_TIMEOUT=90s（legal-mcp 遇司法院 WAF 會 Playwright 暖機約 8 秒，30s 常逾時）；semantic 維持 20s。
- 每日 token 預算：LAWGRAPH_DAILY_TOKEN_LIMIT（程式預設 2000000；**線上自 2026-09-04 設 0＝不限制**，使用者決定暫不設每日額度）、LAWGRAPH_LLM_PAUSED（手動停用）、LAWGRAPH_USAGE_STORE=db＋LAWGRAPH_DB_URL=jdbc:postgresql://postgresql.zeabur.internal:5432/zeabur、LAWGRAPH_DB_USER=${POSTGRES_USERNAME}、LAWGRAPH_DB_PASSWORD=${POSTGRES_PASSWORD}（同專案 postgresql 服務 ID 6a9a72cb73ef6eb935f3166f，含持久卷）。GET /api/usage 可查今日用量與 store。
- 共用資料庫設定已改為 `lawgraph.db.*`（環境變數名稱 LAWGRAPH_DB_URL／USER／PASSWORD 不變），每日用量與 OAuth 憑證共用同一個 PostgreSQL。
- TLR OAuth 零互動（2026-09-04 起）：LAWGRAPH_OAUTH_SESSION_STORE=db 時 refresh token 存 PostgreSQL 資料表 oauth_session；啟動時先用 refresh token 恢復，沒有就由程式自行走 start→authorize→callback（TLR authorize 對已註冊 client 自動同意 302）。retrieve 遇未授權也會先自動授權，失敗 60 秒內不重試；provider 若改成需人工同意，前端授權按鈕仍在。
- 測試省額度：請求帶 header `X-LawGraph-Model`（只接受 LAWGRAPH_TEST_MODEL 這一個值）該案件改用測試模型；`scripts/verify-semantic-live.mjs` 預設帶 gpt-5.4-nano。**Meta Muse 期間線上 LAWGRAPH_TEST_MODEL=muse-spark-1.3-contributor（與 MODEL 相同）**，因此 nano header 會被忽略、一律走 Muse；切回 OpenAI 時把 LAWGRAPH_TEST_MODEL 刪掉即恢復 nano。
- 建置設定：zbpack.json 指向 docker/app/Dockerfile（多階段 Maven 建置，技能包一併 COPY 進映像）
- 流程兩條：LegalGraphAgent／ContractReviewAgent，依 CaseController mode 選；合約步驟 LOAD/QUESTIONS/RESEARCH/REVIEW/SUMMARY/REVISE/GRAPH；前端 hash 路由 #/、#/case、#/contract；示範案例 JSON 以 mode 欄位區分
